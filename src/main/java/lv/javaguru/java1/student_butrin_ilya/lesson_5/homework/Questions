не догадался, как в этом методе использовать конструкции других, более простых методов,
созданных ранее (отдельный метод деления на 3, на 5)
 public String translateNumberToString(int number) {
        String result;
        if (number % 3 == 0 && number % 5 == 0) result = "fizzBuzz";
        else if (number % 3 == 0) result = "fizz";
        else if (number % 5 == 0) result = "Buzz";
        else result = "" + number;
return result;
    }

    2) в 6-м дне дз получилось найти сумму массива через
    int sum = Arrays.stream(mark).sum(); , но не получилось сразу найти
    среднее орифметическое через  double avarage = Arrays.stream(mark).avarage()
    среда разработки просит тип данных OptionalDouble, с которым я не знаком(

    3) как сделать так, чтобы система могла из введенных пользователем данных взять только необходимые значения
    (например пользователь вводит квадратное уравнение, а программа забирает на обработку только
    значения a, b, c)

    4) можно ли внутри класса создать переменную этого же класса?
    5) может ли метод вернуь 2 значения?